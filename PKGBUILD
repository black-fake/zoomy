pkgname="zoomy"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="tool to manage and start (recurring and single) zoom meetings via the command line"
arch=("x86_64")
source=("zoomy")
sha512sums=("SKIP")
depends=("git ripgrep sed awk")
package() {
}
